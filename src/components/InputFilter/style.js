import styled from "styled-components";

export const Container = styled.li`
    display: flex;
    align-items: center;
    gap: .8rem;
    

    label {
        color: #222D39;
        font-weight: 400;
        font-size: 1.6rem;
        line-height: 2.8rem;
    }

    label,
    input {
        cursor: pointer;
    }

`